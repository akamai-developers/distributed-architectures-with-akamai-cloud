apiVersion: v1
kind: Secret
metadata:
  name: order-processor-secret
  namespace: default
type: Opaque
data:
  # Replace with base64-encoded Valkey connection string: echo -n 'valkey://...' | base64
  VALKEY_URL: ""
